# FilmPipe Frontend

Frontend implementation is intentionally out of scope for Agent 1.

Agent 4 should create a minimal desktop-oriented React/Vite UI that talks only to the FilmPipe HTTP API contract.
